/**
 * Get the browser ID from the cookie
 * @param cookieName - The cookie name from global settings
 * @returns The browser ID if the cookie exists
 */
export declare function getBrowserId(cookieName: string): string;
